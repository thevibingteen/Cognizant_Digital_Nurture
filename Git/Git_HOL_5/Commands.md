# Verify repository status
git status

# List all branches
git branch -a

# Switch to main
git checkout main

# Pull latest changes
git pull origin main

# Push local changes
git push origin main

# Verify status
git status

# View commit history
git log --oneline --graph --decorate