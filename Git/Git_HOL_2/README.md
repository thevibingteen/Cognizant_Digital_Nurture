# Git HOL 2

## Objective

Learn how to use `.gitignore` to ignore unwanted files and folders.

## Folder Structure

Git_HOL_2/
├── README.md
├── Commands.md
└── GitDemo/
    ├── .gitignore
    ├── sample.log
    ├── log/
    │   └── app.log
    └── README.txt

## Outcome

Successfully configured `.gitignore` to ignore all `.log` files and the `log` directory.