# Exercise 2 - Verifying Interactions

## Objective

Verify that a mocked object's method is invoked correctly using Mockito.

## Files

- ExternalApi.java
- MyService.java
- MyServiceTest.java

## Result

The test confirms that the expected method on the mocked object is called during execution by using Mockito's verify() method.